import React from "react"

class ProceduralGeneratedMap extends React.Component{



  spanningTree = () => {
    let arr2d = new Array(10).fill(new Array(10).fill(0))
    let open = []
    open.push({x:0,y:0})
    while(open.length){
      arr2d
    }
  }

  render(){
    {this.spanningTree()}
    return(
      <div>

      </div>
    )
  }
}

export default ProceduralGeneratedMap